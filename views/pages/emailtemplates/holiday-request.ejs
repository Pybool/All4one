<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; font-size: 13px; }
    h2 { color: #283078; border-bottom: 1px solid #ccc; padding-bottom: 4px; }
    table { width: 100%; border-collapse: collapse; margin: 10px 0; }
    th, td { border: 1px solid #ccc; padding: 6px; text-align: left; }
    th { background-color: #e0e0e0; }
  </style>
</head>
<body>
  <h1 style="text-align:center;">Holiday Request Form</h1>

  <h2>Employee Information</h2>
  <table>
    <tr><td>Employee Name</td><td><%= data.name %></td></tr>
    <tr><td>Candidate Print Name</td><td><%= data.candidatePrintName %></td></tr>
    <tr><td>Payroll Number</td><td><%= data.payrollNumber || 'N/A' %></td></tr>
  </table>

  <h2>Leave Details</h2>
  <table>
    <tr><td>Number of Days</td><td><%= data.noOfDays %></td></tr>
    <tr><td>From Date</td><td><%= data.fromDate %></td></tr>
    <tr><td>To Date</td><td><%= data.toDate %></td></tr>
    <tr><td>Processing Date</td><td><%= data.processingDate || 'Not provided' %></td></tr>
  </table>

  <h2>Metadata</h2>
  <table>
    <tr><td>Request ID</td><td><%= payload._id %></td></tr>
    <tr><td>Created At</td><td><%= new Date(payload.createdAt).toDateString() %></td></tr>
    <tr><td>Updated At</td><td><%= new Date(payload.updatedAt).toDateString() %></td></tr>
  </table>

  <p style="font-size:12px;color:#777;text-align:center;">
    This document was auto-generated. Signature not required.
  </p>
</body>
</html>
